{
  "name": "hum",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo no build step",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
